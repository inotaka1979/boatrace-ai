# 実装計画

日時: 2026-04-07 14:51:53

**GitHubへの反映は完了していません。** 現状:

1. **ローカルに未push のコミットが2つ** (`485da9c`, `2d3a5b1`) — `origin/main` より2コミット先行
2. **index.html に未コミットの変更** がある（ステージングもされていない）

つまり、前回の検討で議論した「レースごとの個別更新」の設計レビューは**計画段階のみ**で、実装はまだです。また、既存のローカル変更もpushされていません。

### 現在の状態まとめ

| 項目 | 状態 |
|------|------|
| 設計レビュー（P0〜P3） | ✅ 完了（計画のみ） |
| serve_data.py のレース単位API (P0) | ❌ 未実装 |
| index.html の更新ボタン分割 (P1) | ❌ 未実装 |
| ローカルの未pushコミット 2件 | ⚠️ pushが必要 |
| index.html の未コミット変更 | ⚠️ コミット+pushが必要 |

### 次のアクション

実装に進む前に、まず既存の変更をGitHubに反映しますか？ それとも、P0/P1の実装を先に行ってからまとめてpushしますか？